version: '3'

services:
  postgres:
    image: postgres:15
    container_name: pokemon-rpg-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pokemon_rpg
    ports:
      - "5432:5432"
